# === RPC ===
L1_RPC=https://sepolia.drpc.org
L2_RPC=https://sepolia-rpc.giwa.io
RPC_TIMEOUT_MS=60000
L2_RECEIPT_TIMEOUT_MS=600000
L2_POLL_MS=3000
AIRDROP_STRICT_EOA=true
TUI=box
QUIET=true
HUD_BANNER="t.me/Airdropshogun"
HUD_BANNER_COLOR=magenta     # atau hex, mis. #a78bfa
HEADER_FILE=shogun.txt
LOOP_FOREVER=true
ROUND_DELAY_MS=3000


# === Batch & rounds (strict 1–5 wallet per batch) ===
BATCH_SIZE=5      # jumlah wallet paralel per batch (1..5)
ROUNDS=1          # berapa kali putar semua wallet dari awal

# === Deposit (L1 -> L2) ===
SKIP_DEPOSIT=true
DEPOSIT_ETH=0.01
DEPOSIT_L2_GAS=200000

# === Deploy TOKEN (pakai ERC20Lite artifact) ===
ARTIFACT=artifacts/ERC20Lite.json
TOKENS_PER_WALLET=1      # ganti 50 kalau mau 50 token per wallet per round
NAME_BASE=SHOGUN
SYMBOL_BASE=SHOGUN
DECIMALS=18
SUPPLY_HUMAN=50     # total supply awal per token (dalam satuan "manusia")

# === Airdrop token ke EOA random dari blok ===
AIRDROP_COUNT=5          # banyaknya alamat per token
AIRDROP_PER_ADDRESS=10   # jumlah token per alamat (human units)
LOOKBACK_BLOCKS=2000
MAX_SCAN=600

# === Pacing kirim (biar lebih natural & stabil) ===
SLEEP_MS=1500
JITTER_MS=300

HUD_TITLE1=Budak Pekerja 1
HUD_TITLE2=Budak Pekerja 2
HUD_FULL_ADDR=true
HUD_FULL_HASH=false
HUD_MAX_BOX_WIDTH=90

